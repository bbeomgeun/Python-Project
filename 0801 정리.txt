1. 메인 삭제시
우선 디비에서 삭제 -> 모든 위젯 삭제 -> 다시 디비 불러오기로 구현하자

2. add시
화면 업데이트해주자(메인 위젯 삭제 후 다시 디비 불러오기)
--> 업데이트 클래스가 필요하다.

3. delete시
wrapped c++ deleted
-> add할때마다 시그널을 걸어주면 괜찮은데 내 방식은 디비에서 불러오고 마지막에 걸어주는 거라서
이미 삭제한거로 되는듯??

그냥 deletelater대신 디비에서 삭제 후 화면 다시 불러오기로 구현하자.

결론 . 업데이트 메소드든 클래스든 구현해놓자.

+ chkbox 대신 그냥 완료버튼으로 대체하자(시그널 이상함)

update 클래스할때(gridlayout, qtwidget, Qprogressbar, QPushbutton)
로 클래스 정의하고
메소드 delete, add 정의하고

다른곳에서 객체선언하면 작동이 되겠지? 객체.메소드로 업데이트해주고